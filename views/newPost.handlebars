<div id="card" class="card p-3 m-3" id="event-details">
    <div class="card-header form-title text-center text-lg">
        <h2> New Post</h2>
    </div>
    <form class="card-body" id="new-event-form">
        <div class="form-group">
            <label for="post-title" class="form-label">Post Title</label>
            <input name="post-title" class="form-control" type="text" id="post-title">
        </div>
        <div class="form-group">
            <label for="post-body" class="form-label">Write a new post</label>
            <textarea name="post-body" class="form-control" rows="3"></textarea>
        </div>
        
        <button type="submit" class="btn btn-primary">Post it!</button>
    </form>
</div>

<p id="user-name" style="display:none">{{hbsUser.id}}</p>

<script src="/js/newPost.js"></script>
